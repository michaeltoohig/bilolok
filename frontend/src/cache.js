import ls from 'localstorage-slim';

// TODO make a `cache` class that inherits the `ls` object.
// so I can set default values such as a prefix on the key
// handle updating or removing from cache more consistent